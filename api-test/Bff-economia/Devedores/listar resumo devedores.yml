info:
  name: listar resumo devedores
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/devedores-resumo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
