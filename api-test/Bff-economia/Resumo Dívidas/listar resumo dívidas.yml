info:
  name: listar resumo dívidas
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/resumo-dividas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
