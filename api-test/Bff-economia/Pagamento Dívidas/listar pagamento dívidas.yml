info:
  name: listar pagamento dívidas
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/pagamento-dividas/divida/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
