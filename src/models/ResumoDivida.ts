export interface ResumoDivida {
  id: number;
  nome: string;
  totalReceber: number;
  totalRecebido: number;
  saldoReceber: number;
  totalPagar: number;
  totalPago: number;
  saldoPagar: number;
}
